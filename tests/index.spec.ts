import { assert } from 'chai';

describe('calculateDueDate()', () => {
  it('happy path - should return with string represantation of due date', () => {});
  it('should throw error if submit date falls on weekend', () => {});
  it('should throw error if turnaround time is not a number', () => {});
  it('should throw error if submit date is not a valid date string', () => {});
});